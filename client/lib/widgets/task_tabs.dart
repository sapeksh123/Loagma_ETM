// This file previously contained a static TaskList widget with hard-coded
// task cards. Tasks are now loaded dynamically in EmployeeDashboard using
// real data from the backend, so this placeholder widget is no longer used.

